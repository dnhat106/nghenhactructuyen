import React from 'react';
import { Box, Typography } from '@mui/material';

const Categories = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h5">Chủ Đề & Thể Loại</Typography>
    </Box>
  );
};

export default Categories;
